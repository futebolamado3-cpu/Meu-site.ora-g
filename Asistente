
export async function gerarNoticias() {
  const btn = document.getElementById('btn');
  const status = document.getElementById('status');
  const grid = document.getElementById('news-grid');

  btn.disabled = true;
  btn.innerHTML = '<span class="spinner"></span>';
  status.className = 'ai-status visible';
  status.innerHTML = '🤖 IA analisando o conteúdo...';

  // Aqui você pode colocar o skeleton ou buscar notícias reais
  grid.innerHTML = "<p>Carregando notícias...</p>";
}
<script type="module">
  import { gerarNoticias } from './src/asist/gerarNoticias.js';

  window.addEventListener('load', () => {
    setTimeout(gerarNoticias, 800);
  });
</script><!-- AI BAR -->
    <div class="ai-bar">
      <div class="ai-bar-left">
        <div class="ai-icon">🤖</div>
        <div>
          <h3>IA de Notícias — Sport General</h3>
          <p>Powered by Claude · Gera notícias reais de futebol automaticamente</p>
        </div>
      </div>
      <button class="btn-ai" id="btn-gerar" onclick="gerarNoticias()">
        <span>⚡</span> Atualizar Notícias com IA
      </button>
    </div>
    <div class="ai-status" id="ai-status"></div>

    <div id="news-container">
